defaults:
  - pipeline

sweep:
  command_args: >
    --config-name=aa
    inference.ckpt_path=
      ${oc.env:RFDIFFUSION2_WEIGHTS_DIR}/RFD_173.pt
    contigmap.length=150-150
    diffuser.T=50
  benchmark_json: training_benchmarks.json
  benchmarks: tip_4_lig_retroaldolase_pa
  num_per_condition: 2
  num_per_job: 2

outdir: /net/scratch/${oc.env:USER}/se3_diffusion/benchmarks/${now:%Y-%m-%d}_${now:%H-%M-%S}_retroaldolase_demo
